# `configs/protocol_1`

Canonical imported configs for `results/protocol_1`, the protocol-
hyperparameter experiment namespace.

- `finetune/`: configs matching `results/protocol_1/*/*/finetune_last2/`
- `from_scratch/`: configs matching `results/protocol_1/*/*/train_alignn_from_scratch/`

These configs correspond to:

- `epochs = 50`
- `batch_size = 16`
- `learning_rate = 0.0001`
