# `configs/protocol_2`

Canonical imported configs for `results/protocol_2`, the ALIGNN-
recommended experiment namespace.

- `finetune/`: configs matching `results/protocol_2/*/*/finetune_last2/`
- `from_scratch/`: configs matching `results/protocol_2/*/*/train_alignn_from_scratch/`

These configs correspond to:

- `epochs = 300`
- `batch_size = 64`
- `learning_rate = 0.001`
